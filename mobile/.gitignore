# Flutter/Dart generated files — created by `flutter create .` / `flutter pub get`
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/

# Platform folders are regenerated by `flutter create .` — safe to ignore
# until you've run that command locally; remove these two lines from this
# file once android/ and ios/ exist and you want to commit them.
/android/
/ios/

# Environment/secrets
.env
